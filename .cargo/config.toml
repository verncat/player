# Keep Android toolchain selection host-specific.
# Tauri Android tooling or cargo-ndk should provide the correct NDK linker
# and C toolchain for the current machine. If local overrides are needed,
# put them in ~/.cargo/config.toml instead of committing them here.
